# Producción

Aquí va la misma superficie que `../simulado`, contra el proyecto Supabase
`zcwueglgirqxwazhfidi`, cuando existan las columnas listadas en
`src/tipos/index.ts` bajo «pendiente de migración».

Los servicios de `src/servicios/*.ts` no cambian: solo cambia la línea
`export const fuente = ...` de `../index.ts`.
